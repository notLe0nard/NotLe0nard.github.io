function reloadPage() {
    location.reload();
}
  
  // Set the interval in milliseconds (1000 milliseconds = 1 second)
  const interval = 10 * 1000; // Change this value to your desired interval
  
  setInterval(reloadPage, interval);
  